import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  
}

const motivateSlice = createSlice({
  name: "loader",
  initialState,
  reducers: {

  }
});

export const {} = motivateSlice.actions;

export default motivateSlice.reducer;
